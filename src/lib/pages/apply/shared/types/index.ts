/**
 * Shared types for apply module
 * Re-exports types used across featured and digital-card applications
 */

// Re-export from featured types
export type {
  SubmittedFile,
  FormErrors,
  GetFeaturedFormData,
  ValidationResult,
  TabValidationState
} from '../../featured/types';
